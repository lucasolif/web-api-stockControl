# Conexão com MySQL local
spring.datasource.url=jdbc:mysql://localhost:3306/stockcontrol?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=D8hj0ptr
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração do JPA
spring.jpa.hibernate.ddl-auto=update

# Configuração Hibernate
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Configuração JTW
jwt.secret=K85#$@#D8Q2ukda8419!$lo@!od541d47waSbcasPOWfregr#5DkL2vXsT7@xR4y
jwt.issuer=autoflex-api
jwt.expirationMinutes=60

# Configucação CORS
app.cors.allowed-origins=http://localhost:4200

# Configuração Contex-Path
server.servlet.context-path=/autoflex